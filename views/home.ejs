<h1>
    Hello <%=data.username%>!!
    <p>Current version is <%=data.version%></p>
</h1>

<div>
    <!-- <p class="lead">
        We have <strong><%=data.length%></strong> users.. 
    </p>
    
    <form method="POST" action='/register'>
        <input type="text" class="form" name="email" placeholder="Enter Your Email">
        <button>Join Now</button>
    </form>

    <table>
            <tr>
                <th>Email</th><th>Time</th>
            </tr>
            <% for (var i = 0; i < data.length; i++) { %>
                <tr>
                    <td><%= data[i].email %></td>
                    <td><%= data[i].created_at %></td>
                </tr>    
            <% } %>
    </table> -->
         
</div>